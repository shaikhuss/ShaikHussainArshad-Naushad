//----------------------------------------------------------------------------
// Copyright 2020, Ed Keenan, all rights reserved.
//----------------------------------------------------------------------------

#ifndef XAUDIO_LOAD_LIBS_H
#define XAUDIO_LOAD_LIBS_H

    // Using a pragma... otherwise we need to modify the project settings
	// Load the static library
    // if this doesn't work try XAudio2_9.lib
	// #pragma comment (lib, "XAudio2_8.lib")

	// Is loading the lib needed?
	// Might be part of windows project

#endif

// End of File 
